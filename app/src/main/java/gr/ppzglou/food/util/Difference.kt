package gr.ppzglou.food.util

/*
import androidx.recyclerview.widget.DiffUtil
import com.ppzglou.eforologiki.data.models.DependentMemberModel
import com.ppzglou.eforologiki.data.models.FileModel
import com.ppzglou.eforologiki.data.models.MessagesModel

val STATES_DIFF_UTIL = object : DiffUtil.ItemCallback<Int>() {
    override fun areItemsTheSame(oldItem: Int, newItem: Int): Boolean =
        oldItem == newItem

    override fun areContentsTheSame(oldItem: Int, newItem: Int): Boolean =
        oldItem == newItem
}

val POINT_STATES_DIFF_UTIL = object : DiffUtil.ItemCallback<Int>() {
    override fun areItemsTheSame(oldItem: Int, newItem: Int): Boolean =
        oldItem == newItem

    override fun areContentsTheSame(oldItem: Int, newItem: Int): Boolean =
        oldItem == newItem
}

val MEMBERS_DIFF_UTIL = object : DiffUtil.ItemCallback<DependentMemberModel>() {
    override fun areItemsTheSame(
        oldItem: DependentMemberModel,
        newItem: DependentMemberModel
    ): Boolean =
        oldItem.amka == newItem.amka

    override fun areContentsTheSame(
        oldItem: DependentMemberModel,
        newItem: DependentMemberModel
    ): Boolean =
        oldItem == newItem
}

val FILES_DIFF_UTIL = object : DiffUtil.ItemCallback<FileModel>() {
    override fun areItemsTheSame(oldItem: FileModel, newItem: FileModel): Boolean =
        oldItem.url == newItem.url

    override fun areContentsTheSame(oldItem: FileModel, newItem: FileModel): Boolean =
        oldItem == newItem
}

val CHAT_DIFF_UTIL = object : DiffUtil.ItemCallback<MessagesModel>() {
    override fun areItemsTheSame(oldItem: MessagesModel, newItem: MessagesModel): Boolean =
        oldItem.time == newItem.time

    override fun areContentsTheSame(oldItem: MessagesModel, newItem: MessagesModel): Boolean =
        oldItem == newItem
}
*/
